Если спросят "в чём суть проекта" — ответ:
Проект демонстрирует применение готовых ML-моделей и LLM для создания интеллектуального помощника, работающего полностью локально.

Если спросят "что делает агент" — ответ:
Он анализирует локальные документы, ищет релевантные фрагменты и формирует текстовый ответ с помощью языковой модели.

Если спросят "в чём отличие от ChatGPT" — ответ:
Агент работает без интернета, ограничен локальной базой знаний и не обучается на внешних данных.
